# Booking edit / 2-state status / color unification

## Objective
Three user-reported issues in reservation management:
1. Editing a booking silently drops changes to property/client (looks editable, isn't).
2. Booking status has 5 live values (`pending`, `confirmed`, `active`, `completed`, `cancelled`); user wants exactly 2 (`confirmed`, `cancelled`), auto-confirmed on creation, with a real cancel action that's recorded (not a hard delete).
3. Three inconsistent, uncoordinated color systems across Calendar grid/list, BookingDetailModal, and the new Dashboard availability heatmap confuse "reserved" vs "free" vs "who".

## Why
Root causes verified by direct code reading (not assumed):
- `BookingUpdate` schema (`backend/app/schemas/schemas.py:78-99`) omits `property_id`/`client_id`; Pydantic's default `extra="ignore"` silently drops them from the PUT body before `update_booking` ever sees them.
- `Booking.status` (`backend/app/models/models.py:67`) is a free string, default `'pending'`, with 5 values in live use; no cancel endpoint exists anywhere — only a hard `DELETE` (`backend/app/api/endpoints.py:443-454`).
- Calendar grid colors by `client_id` hash (`frontend/src/pages/Calendar.tsx:362`, `frontend/src/utils/entityColor.ts`), Calendar list/BookingDetailModal color by property + separate status badge, Dashboard heatmap (`frontend/src/pages/Dashboard.tsx:140-209`) uses a third, independent green/red free/occupied scheme with no visible legend.

## Design decisions (already made, do not re-derive)
- **Status stays exactly 2 values**: `confirmed`, `cancelled`. Existing `pending`/`active`/`completed` rows migrate to `confirmed` (data backfill in the migration).
- **"Completed"/checked-out is NOT a status value anymore** — it becomes a new nullable `checked_out_at` timestamp column on `Booking`. This preserves every existing feature that depended on "completed" (Finance.tsx revenue movements table, Calendar's checkout-button gating, Dashboard's `getStatusChip`) without reintroducing a 3rd status. UI may still show a "Finalizada" indicator, but it's *derived* from `checked_out_at != null`, not a stored status.
- **Cancellation is a new dedicated endpoint** `POST /bookings/{id}/cancel`, not a generic status PATCH. It sets `status='cancelled'` and a new `cancelled_at` timestamp (this is the audit trail the user asked for — "que quede registrado"). The existing hard-delete stays as-is (unrelated cleanup action, not touched).
- **`status` is removed from `BookingCreate` and `BookingUpdate`** entirely — the server always sets `status='confirmed'` on creation; the only way to reach `cancelled` is the new cancel endpoint. This closes the surface where a client could otherwise set arbitrary status.
- **Colors unify on property color** (`Property.color`, already configurable in `PropertyModal.tsx`) everywhere a reservation block/card is shown: Calendar grid, Calendar list, BookingDetailModal header already does this for list — grid must switch from client-hash to property color to match. Dashboard heatmap's green=free/red=occupied binary scheme is kept as-is (it already signals "reserved" clearly) but gets a small visible legend added (currently only a hover tooltip).

## Verified current-state facts the implementer needs (do not re-explore)
- DB is Postgres (`asyncpg`), Alembic migrations live in `backend/migrations/versions/`, script_location = `migrations`. Current head revision: `9781ed5ede12` (chain: `4a2e0323ff73` → `750ae7b221c4` → `9f3c2a7d5e1b` → `3d8b1f6c4a29` → `9781ed5ede12`).
- `Booking` model fields relevant here: `backend/app/models/models.py:33-76` (`status` at line 67, no `checked_out_at`/`cancelled_at` yet).
- `BookingCreate`/`BookingUpdate`/`BookingResponse` in `backend/app/schemas/schemas.py:57-99` and `:7-43` respectively.
- `create_booking` (`endpoints.py:76-166`) already does overlap-conflict validation via `overlap_query` (lines 89-102) — `update_booking` (`endpoints.py:169-246`) does NOT; it needs an equivalent check whenever `property_id`/`check_in`/`check_out` change, excluding the booking's own id from the conflict query.
- All booking-status filters in `endpoints.py` (grep-verified, exhaustive list): lines 60, 92, 142, 734, 743, 804, 813, 833, 843, 919, 950, 1118, 1218, 1345.
  - `Booking.status != 'cancelled'` (92, 804, 813, 843, 919, 950) — stays as-is, still correct under 2-value status.
  - `Booking.status.in_(['confirmed','active','completed'])` (734, 743, 1118, 1218) — becomes `Booking.status == 'confirmed'`.
  - `Booking.status.notin_(['cancelled','completed'])` (833, `active_bookings_query`) — becomes `and_(Booking.status != 'cancelled', Booking.checked_out_at.is_(None))`.
  - Line 142 (`status=booking_data.status` in `create_booking`) — becomes hardcoded `status='confirmed'`.
  - Line 1345 (CSV import, `booking_data.get('status', 'pending')`) — normalize: any value whose `.lower() == 'cancelled'` maps to `'cancelled'`, everything else maps to `'confirmed'`. Line 1347's `service_status` derivation must keep reading the *raw* imported string (unrelated field, don't change its logic) — only the value stored in `Booking.status` gets normalized.
- `GET /bookings` (`endpoints.py:45-73`) takes an optional `status` query param. Finance.tsx's only special caller is `getBookings('completed')` (`frontend/src/pages/Finance.tsx:114`) — this must change since `'completed'` will no longer be a status value. Add a second optional query param `checked_out: bool = None` to `GET /bookings`; when true, filter `Booking.checked_out_at.isnot(None)`. Update `frontend/src/services/api.ts`'s `getBookings` to accept `(status?: string, checkedOut?: boolean)` and pass `checked_out` through; update the one call site in `Finance.tsx:114` to `getBookings(undefined, true)`. All other callers (`Clients.tsx:63`, `Properties.tsx:91`, `Calendar.tsx:97`, `Dashboard.tsx:238`, `Layout.tsx:125`) call `getBookings()` with no args — do not touch them.
- `CheckoutModal.tsx:59-62` currently sends `{ status: 'completed', checkout_notes }` via the generic `updateBooking`. Change to `{ checked_out_at: <ISO now>, checkout_notes }`.
- `frontend/src/services/api.ts` `BookingPayload` interface (lines 4-28) already declares `property_id?`/`client_id?` (contrary to an earlier bad report — verified directly, they ARE there) — no frontend payload-shape change needed there beyond adding `checked_out_at?: string` (already covered by generic `status?`/`checkout_notes?` pattern nearby).
- `frontend/src/components/BookingModal.tsx` form already renders `property_id`/`client_id` as freely editable in both create and edit mode (lines 205-235) — **no frontend change needed for issue #1**, it was always a backend-only bug (dropped fields). Verify this after the backend fix by checking a booking's `property_id` actually changes on edit.
- Calendar.tsx: grid coloring at line 362 (`const color = getEntityColor(b.client_id)`), legend at lines 253-262/312-321 (`visibleClients`), `getBookingsForDay` filter at line 231 (`if (b.status === 'cancelled' || b.status === 'completed') return false;` — update to use `checked_out_at` instead of `'completed'`), `getStatusColor`/`STATUS_LABELS` at lines 188-205 (reduce to 2 real statuses + keep a separate "Finalizada" treatment driven by `checked_out_at`).
- `frontend/src/components/BookingDetailModal.tsx` uses `getStatusColor` prop (passed from Calendar.tsx) and has `isCompleted = booking.status === 'completed'` (line 51) gating the checkout button — must become `!!booking.checked_out_at`. Needs `checked_out_at` added to its `DetailBooking` interface (line 4-19).
- `frontend/src/pages/Dashboard.tsx`: `getStatusChip` (lines 75-83, reads `status === 'completed'`/`'active'`/`'pending'`) and 3 more status filters at lines 301, 322, 341, 353, 359, 364 (same `!= 'cancelled'` / `notin(['cancelled','completed'])` pattern as backend — same translation rule: `'completed'` membership → `checked_out_at` check). `SeasonAvailabilityWidget` (lines 140-209) keeps its green/free-red/occupied dot scheme; add a small legend (green dot "Libre" / red dot "Reservado") near the widget header — it currently has none, only per-dot hover tooltips.
- `frontend/src/components/ClientModal.tsx`: `STATUS_LABELS` (lines 30-36) and its use at line 133 — reduce to confirmed/cancelled + derive "Finalizada" from `checked_out_at` on `ClientBooking` (interface at lines 7-15, needs `checked_out_at?: string` added). Need to check where `bookings` prop is populated (`Clients.tsx`) to ensure `checked_out_at` is passed through — it will be, since it comes straight from the API response once added to `BookingResponse`.
- `frontend/src/utils/entityColor.ts` — no change needed, `getColorByKey`/`getEntityColor` already support property-color lookup with hash fallback; Calendar grid just needs to call it with `property_id` instead of `client_id` (mirror the list view's existing `getColorByKey(property?.color) ?? getEntityColor(b.property_id)` pattern at `Calendar.tsx:398`).
- `PropertyModal.tsx` color picker / `Property.color` column — untouched, already exists and is the source of truth for property colors.
- Import.tsx (frontend CSV import screen) builds its own `status: 'active'|'pending'` string client-side purely to drive the backend's `service_status` derivation (`Import.tsx:121`) — **do not touch this file**, the backend-side normalization described above handles it correctly without any frontend change.

## Scope boundaries
- Do NOT touch `Property.status` (`PropertyModal.tsx`) — unrelated field, same name, different entity.
- Do NOT remove the existing hard-delete ("Eliminar") button/endpoint — keep it alongside the new "Cancelar" button.
- Do NOT invent a test framework. No backend tests exist (`find backend -iname test_*.py` → empty, no pytest installed, no venv found), no frontend tests exist (`find frontend/src -iname *.test.*` → empty, no test script in package.json). Repo has "Strict TDD Mode: enabled" configured but no runner to execute it against — disclose this gap in the final report rather than fabricating a framework. Functional verification available: `cd frontend && npm run build` (tsc -b + vite build) and `npm run lint`; backend has no way to run/typecheck in this environment beyond `python3 -m py_compile` on edited files (no venv, no `python`/`pytest` binary found) — note this limitation explicitly.
- No AI attribution in commits (user's CLAUDE.md rule overrides the default attribution reminder). Use Conventional Commits only, in English for the commit message body per this project's existing commit style (check `git log` for the pattern already in use, e.g. `feat(dashboard): ...`).

## Tasks
- [x] T1 — Alembic migration: add `checked_out_at`, `cancelled_at` (nullable `TIMESTAMP(timezone=True)`) to `bookings`; backfill (`completed`→sets `checked_out_at`, `cancelled`→sets `cancelled_at`, then collapse `pending`/`active`/`completed` status values to `confirmed`); change column default to `'confirmed'`. Chain from head `9781ed5ede12`. Follow the style of `backend/migrations/versions/9781ed5ede12_add_user_password_seed_demo_account.py` (docstring, `op.execute` for backfill, `upgrade`/`downgrade`).
- [x] T2 — `models.py`: add `checked_out_at`, `cancelled_at` columns to `Booking`; change `status` default to `'confirmed'`.
- [x] T3 — `schemas.py`: remove `status` from `BookingCreate`; add `property_id: Optional[str]`, `client_id: Optional[str]`, `checked_out_at: Optional[datetime]` to `BookingUpdate` (remove `status` from `BookingUpdate` too — cancellation goes through the dedicated endpoint only); add `checked_out_at`, `cancelled_at` to `BookingResponse` (with datetime serialization consistent with `created_at`).
- [x] T4 — `endpoints.py`: hardcode `status='confirmed'` in `create_booking`; add UUID casting for `property_id`/`client_id` in `update_booking`'s generic setattr loop; add overlap-conflict validation to `update_booking` (reuse `create_booking`'s `overlap_query` pattern, excluding the booking's own id, only run when `property_id`/`check_in`/`check_out` are among the updated fields); add `POST /bookings/{booking_id}/cancel` endpoint (sets `status='cancelled'`, `cancelled_at=now`, returns `BookingResponse` with joined `property_name`/`client_name` like the other endpoints); translate every status filter listed above; add `checked_out: bool = None` query param to `get_bookings`; normalize CSV import status per the rule above.
- [ ] T5 — `frontend/src/services/api.ts`: add `checked_out_at?: string`, `cancelled_at?: string` to `BookingPayload`; add `cancelBooking(id: string)` calling `POST /bookings/{id}/cancel`; extend `getBookings(status?: string, checkedOut?: boolean)`.
- [ ] T6 — `CheckoutModal.tsx`: send `checked_out_at` instead of `status: 'completed'`.
- [ ] T7 — `BookingDetailModal.tsx`: add `checked_out_at?`/`cancelled_at?` to `DetailBooking`; `isCompleted` derives from `checked_out_at`; add a "Cancelar reserva" button (calls a new `onCancel` prop) next to/near "Eliminar", styled distinctly (e.g. warning tone, not destructive-red like delete) with a confirm step (reuse the existing `ConfirmModal` pattern from Calendar.tsx); `canCheckout`/status badge logic updated for 2-value status.
- [ ] T8 — `Calendar.tsx`: wire `cancelBooking` + a confirm dialog (mirror `handleConfirmDelete`) and pass `onCancel` to `BookingDetailModal`; switch grid-cell coloring from `getEntityColor(b.client_id)` to property color (mirror list view's existing pattern); switch the legend from per-client to per-property; update `getBookingsForDay`'s exclusion filter and `STATUS_LABELS`/`getStatusColor` for the 2-value status + derived "Finalizada".
- [ ] T9 — `Dashboard.tsx`: update `getStatusChip` and the `notin(['cancelled','completed'])`-style filters (lines 301, 322, 341, 353, 359, 364) to use `checked_out_at`; add a small visible legend (green="Libre"/red="Reservado") to `SeasonAvailabilityWidget`.
- [ ] T10 — `ClientModal.tsx`: reduce `STATUS_LABELS`, derive "Finalizada" from `checked_out_at` on `ClientBooking`.
- [ ] T11 — `Finance.tsx`: change `getBookings('completed')` call to `getBookings(undefined, true)`.
- [ ] T12 — Verify: `cd frontend && npm run build && npm run lint`; `python3 -m py_compile` on every edited backend `.py` file; manually trace one edit-a-booking-changing-property scenario through the code to confirm the fix (can't run the app live — no DB configured in this environment — so this is a code trace, not a live test; disclose that).

## Progress

### Backend (T1-T4)
- New migration `backend/migrations/versions/a5c1e9f04d3b_booking_status_checkout_cancel.py`, revises `9781ed5ede12`. Adds `checked_out_at`/`cancelled_at`, backfills from old status values (using `updated_at` as the checkout/cancel timestamp approximation, since no better historical signal exists), collapses `pending`/`active`/`completed` → `confirmed`, sets new column default `'confirmed'`.
- `models.py`: `Booking.checked_out_at`, `Booking.cancelled_at` added; `status` default changed to `'confirmed'`.
- `schemas.py`: `BookingCreate.status` removed; `BookingUpdate` gained `property_id`, `client_id`, `checked_out_at` and lost `status`; `BookingResponse` gained `checked_out_at`, `cancelled_at`.
- `endpoints.py`:
  - `create_booking` hardcodes `status='confirmed'`.
  - `update_booking` now UUID-casts `property_id`/`client_id` in the setattr loop (this is the fix for issue #1 — previously `BookingUpdate` didn't even declare these fields, so Pydantic's default `extra="ignore"` silently dropped them before `update_booking` ever saw them) and runs an overlap-conflict check (mirrors `create_booking`'s `overlap_query`, excludes the booking's own id) whenever `property_id`/`check_in`/`check_out` are in the update payload.
  - New `POST /bookings/{booking_id}/cancel` sets `status='cancelled'` + `cancelled_at=now(utc)`, returns the joined `BookingResponse`.
  - `GET /bookings` gained `checked_out: bool = None` param, filtering `checked_out_at is/isnot None`.
  - All `Booking.status.in_(['confirmed','active','completed'])` occurrences (dashboard totals x2, accounting stats x2) → `Booking.status == 'confirmed'`. The one `notin_(['cancelled','completed'])` (active_bookings_query) → `status != 'cancelled'` AND `checked_out_at.is_(None)`. All `!= 'cancelled'` filters left untouched (still correct).
  - CSV import: `status` now normalizes to `'cancelled'` if the raw imported string lowercases to `'cancelled'`, else `'confirmed'`. `service_status` derivation on the next line still reads the raw imported string, untouched per spec.
- Verification: `python3 -m py_compile backend/app/models/models.py backend/app/schemas/schemas.py backend/app/api/endpoints.py backend/migrations/versions/a5c1e9f04d3b_booking_status_checkout_cancel.py` → passed, no output (`PY_COMPILE_OK`).
- Could NOT verify: no venv/pytest/DB in this environment, so no migration run, no live query execution, no import test. Compile-only check.

## Delivery
Single feature branch off `feat/dashboard-property-availability-heatmap` (current branch, clean). Work-unit commits per logical group (migration+model+schema+endpoints as one backend commit, frontend as one or two commits), Conventional Commits, no AI attribution trailer. No push/PR without separate user confirmation.
